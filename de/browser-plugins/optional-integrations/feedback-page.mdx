---
sidebarTitle: "Benutzerfeedback"
title: "Sammle Feedback von deinen Benutzern"
icon: "comment"
robots: "index, follow, max-snippet:-1, max-image-preview:large, max-video-preview:-1"
canonical: "https://mellowtel.com/docs/de/browser-plugins/optional-integrations/feedback-page"
og:title: "Mellowtel Feedback-Seite für Browser-Erweiterungen Anleitung"
og:description: "Die Einrichtung der Mellowtel Feedback-Seite zeigt, wie Browser-Plugins Deinstallations-Feedback-Links generieren, eine gehostete Seite öffnen und Antworten im Dashboard überprüfen können."
og:url: "https://mellowtel.com/docs/de/browser-plugins/optional-integrations/feedback-page"
twitter:card: "summary"
twitter:title: "Mellowtel Feedback-Seite für Browser-Erweiterungen Anleitung"
twitter:description: "Die Einrichtung der Mellowtel Feedback-Seite zeigt, wie Browser-Plugins Deinstallations-Feedback-Links generieren, eine gehostete Seite öffnen und Antworten im Dashboard überprüfen können."
---

Mellowtel bietet eine vorgefertigte Feedback-Seite, die du nutzen kannst, um Feedback von deinen Benutzern zu sammeln, damit du schneller iterieren und dein Produkt verbessern kannst. Derzeit ist die Seite nur für Browser-Plugins verfügbar. Die Feedback-Seite ist ähnlich wie diese [hier](https://www.mellowtel.com/uninstall-feedback/?extension_id=testId&configuration_key=testConfiguration), indem du das Plugin-Icon und den Namen dynamisch übergibst.

Du kannst das Feedback dann vom Mellowtel-Dashboard aus lesen und es nutzen, um dein Browser-Plugin zu verbessern.

WICHTIG: Du kannst das Feedback nur dann vom Mellowtel-Dashboard aus lesen, wenn du ein Plugin hast, das im Web Store veröffentlicht ist und nicht im Entwicklungsmodus. Wenn du noch in der Entwicklung bist und die Feedback-Seite testen möchtest, kontaktiere uns bitte über den Chat.

So kannst du die Feedback-Seite zu deinem Browser-Plugin hinzufügen:


## Füge die Feedback-Seite zu deinem Plugin hinzu

Die Feedback-Seite wird von Mellowtel gehostet und ist über eine URL zugänglich, die du mit der `generateFeedbackLink()`-Methode aus dem Mellowtel JS-Paket generieren kannst.

Wenn du die Feedback-Seite direkt in einem neuen Tab öffnen möchtest, kannst du die `generateAndOpenFeedbackLink()`-Methode verwenden, und es wird eine Seite ähnlich wie diese [hier](https://www.mellowtel.com/uninstall-feedback/?extension_id=testId&configuration_key=testConfiguration) geöffnet, indem du das Plugin-Icon und den Namen dynamisch übergibst.

Du kannst die Feedback-Seite öffnen, indem du die `generateFeedbackLink()`-Methode aus dem Hintergrundskript oder Inhaltsskript deiner Erweiterung aufrufst, wenn ein Benutzer dein Browser-Plugin deinstalliert.

So kannst du es machen.

Öffne deine `service worker`-Datei (auch bekannt als Hintergrundskript) und füge den folgenden Code hinzu:

Denke daran, `<YOUR_CONFIGURATION_KEY>` durch deinen tatsächlichen Mellowtel-Konfigurationsschlüssel zu ersetzen. Wenn du noch keinen Konfigurationsschlüssel hast, kannst du einen erhalten, indem du dich bei [Mellowtel](https://www.mellowtel.com/mellowtel-auth/) anmeldest.

```javascript
import Mellowtel from "mellowtel";

let mellowtel;

(async () => {
    mellowtel = new Mellowtel("<YOUR_CONFIGURATION_KEY>");
    await mellowtel.initBackground();
})();

// Öffne die Feedback-Seite, wenn der Benutzer die Erweiterung deinstalliert
chrome.runtime.onInstalled.addListener(async function(details) {
    console.log("Erweiterung installiert oder aktualisiert");
    // Wenn du Installation und Updates unterschiedlich behandeln möchtest
    /**
    if(details.reason === "install"){
        // rufe eine Funktion auf, um eine Erstinstallation zu behandeln
    } else if(details.reason === "update") {
        // rufe eine Funktion auf, um ein Update zu behandeln
    }
    **/
    // await mellowtel.generateAndOpenOptInLink(); um Benutzereinstellungen zu bearbeiten. Für weitere Details: Opt-In-Out-Dokumentation
    const uninstallURl = await mellowtel.generateFeedbackLink();
    chrome.runtime.setUninstallURL(uninstallURl);
});
```